# 環境状態台帳(state.md)

各章を書き終えた時点で「その章終了時点に存在するリソースと設定」を追記していく台帳です。次章の執筆・レビュー時は、まずこのファイルで前章までの到達状態を確認します。

- 各章のセクションには「作成したリソース / RBAC・ポリシー変更 / ネットワーク変更 / コスト影響」を表で記録します。
- ハンズオンの内容と食い違いが出ないよう、章のマージ前に必ず更新します(CLAUDE.md のルール)。

---

## 第1章 終了時点

第1章では、土台の「器の骨格」を確定させました。dev/prod の分離方式(サブスクリプション分離を主軸)・命名規則(CAF 準拠)・主リージョン(東日本)を決め、最初のリソースグループを作成しています。

### 決めた方針(以降の章の前提)

| 項目 | 決定内容 |
|---|---|
| dev/prod 分離方式 | サブスクリプション分離を主軸(個人学習はリソースグループ分離に読み替え。付録E) |
| 命名規則 | CAF 準拠 `{種別}-{ワークロード}-{環境}-{リージョン}-{連番}`、ワークロード名は `foundation` |
| 主リージョン | 東日本(`japaneast`) |
| 必須タグ(項目のみ決定) | `env` / `owner`(強制は第3章のポリシーで実施) |

### 作成したリソース

| リソース種別 | 名前 | スコープ | 備考 |
|---|---|---|---|
| 管理グループ | `mg-foundation` | テナント | dev/prod サブスクリプションを束ねる統制の器 |
| リソースグループ | `rg-foundation-dev` | サブスクリプション(dev) | 東日本。タグ `env=dev` / `owner` |
| リソースグループ | `rg-foundation-prod` | サブスクリプション(prod) | 東日本。タグ `env=prod` / `owner` |

### RBAC・ポリシー変更

| 対象 | 変更内容 | スコープ |
|---|---|---|
| — | この章では未実施(第2章で RBAC、第3章でポリシーを設計) | — |

### ネットワーク変更

| 対象 | 変更内容 |
|---|---|
| — | この章では未実施(第4章で VNet を構築) |

### コスト影響

| 項目 | 概算 | 備考 |
|---|---|---|
| 管理グループ / リソースグループ | 無料 | 器自体に課金はない。中に置くリソースから課金が始まる |
| 学習用サブスクリプション | 従量課金 | 消し忘れに注意。予算アラートは第5章・付録D で設定 |

---

## 第2章 終了時点

第2章では、第1章で作った器の骨格に「ID と権限の層」を足しました。テナント(Entra ID)に管理者/開発者/閲覧者の 3 グループを作成し、dev/prod へ強さの違う RBAC を割り当てました。dev は緩く、prod は厳しく、という差を権限で表現しています。個人への直接割り当てを避け、グループ経由で付与する原則を確立しました。

### 決めた方針(以降の章の前提)

| 項目 | 決定内容 |
|---|---|
| 権限付与の原則 | 個人へ直接割り当てず、グループ経由で付与する |
| ロールの既定 | 既定は共同作成者。所有者は絞る |
| 本番特権の扱い | 本番の管理者権限は常時付与せず PIM で一時昇格(実運用時に導入。P2 前提) |
| 割り当てスコープ | 本書はまずサブスクリプション スコープで割り当てる |

### 作成したリソース

| リソース種別 | 名前 | スコープ | 備考 |
|---|---|---|---|
| セキュリティグループ | `grp-foundation-admins` | テナント(Entra ID) | 管理者。本番所有者は PIM で一時昇格する運用を想定 |
| セキュリティグループ | `grp-foundation-developers` | テナント(Entra ID) | 開発者。dev=共同作成者 / prod=閲覧者 |
| セキュリティグループ | `grp-foundation-viewers` | テナント(Entra ID) | 閲覧者。dev/prod とも閲覧者 |

### RBAC・ポリシー変更

| 対象 | 変更内容 | スコープ |
|---|---|---|
| `grp-foundation-developers` | 共同作成者(Contributor)を割り当て | サブスクリプション `sub-foundation-dev` |
| `grp-foundation-developers` | 閲覧者(Reader)を割り当て | サブスクリプション `sub-foundation-prod` |
| `grp-foundation-viewers` | 閲覧者(Reader)を割り当て | `sub-foundation-dev` / `sub-foundation-prod` |
| `grp-foundation-admins` | 本番所有者は常時付与せず PIM で一時昇格(本章では常時割り当てを実施せず) | `sub-foundation-prod` |

### ネットワーク変更

| 対象 | 変更内容 |
|---|---|
| — | この章では未実施(第4章で VNet を構築) |

### コスト影響

| 項目 | 概算 | 備考 |
|---|---|---|
| Entra ID グループ / ロール割り当て | 無料 | グループ・RBAC 自体に課金はない |
| 動的グループ / PIM | ライセンス要 | 動的グループは P1、PIM は P2 を前提とする(学習段階では不要) |

## 第3章 終了時点

第3章では、器と権限の層に「ガバナンス(ポリシー)の層」を足しました。管理グループ `mg-foundation` に共通ポリシー(必須タグ・許可リージョン)を、prod サブスクリプションに追加の厳しいポリシー(パブリック IP 作成の禁止)を割り当て、継承で配下へ効かせます。第2章の合言葉「dev は緩く、prod は厳しく」を、今度は権限でなく「作れるリソースの範囲」で表現しました。必須タグは監査で現状把握してから拒否へ引き上げる、段階導入の型を確立しています。

### 決めた方針(以降の章の前提)

| 項目 | 決定内容 |
|---|---|
| ポリシー導入の順序 | 監査(Audit)で現状把握 → 既存違反を修正 → 拒否(Deny)へ引き上げ |
| 割り当てスコープの原則 | 共通ルールは管理グループへ、環境差(prod 限定)は個別サブスクリプションへ |
| 例外の扱い | 除外(Exemption)で対象・理由・期限を添えて登録。恒久的な穴を作らない |
| 必須タグの強制 | 第1章で項目のみ決めた `env` / `owner` を、本章でポリシーとして強制 |

### 作成したリソース

| リソース種別 | 名前 | スコープ | 備考 |
|---|---|---|---|
| ポリシー割り当て | `require-tag-env-deny`(必須タグ env) | サブスクリプション `sub-foundation-prod` | 監査で導入後、拒否へ引き上げ。ハンズオンで実施 |

### RBAC・ポリシー変更

| 対象 | 変更内容 | スコープ |
|---|---|---|
| 必須タグ(`env` / `owner`) | 監査 → 拒否で割り当て(共通) | 管理グループ `mg-foundation` |
| 許可リージョンを東日本に限定 | 拒否で割り当て(共通) | 管理グループ `mg-foundation` |
| パブリック IP の作成を禁止 | 拒否で割り当て(prod 限定) | サブスクリプション `sub-foundation-prod` |

> ハンズオンでは必須タグ(env)の prod への割り当てを実施。管理グループへの共通ポリシーと prod のパブリック IP 禁止は、同じ要領で割り当てる設計として提示。

### ネットワーク変更

| 対象 | 変更内容 |
|---|---|
| — | この章では未実施(第4章で VNet を構築)。ただしパブリック IP 作成の禁止ポリシーは第4章以降の構築に影響する |

### コスト影響

| 項目 | 概算 | 備考 |
|---|---|---|
| Azure Policy(定義・割り当て・評価) | 無料 | ポリシー機能自体に課金はない |

## 第4章 終了時点

第 4 章では、統制の層(第 1〜3 章)の上に「ネットワークの層」を足しました。将来の Hub-Spoke 化・オンプレミス接続を見越した CIDR 設計を先に固め、dev/prod へ VNet を 1 つずつ構築しています。各 VNet は app 用・data 用の 2 サブネットと Bastion 専用サブネットを持ち、NSG はサブネット単位で管理します。既定を拒否寄りにし、data サブネットは app サブネットからのみ受信を許可する設計です。dev では一時 VM と Azure Bastion で疎通を確認しました(確認後に削除)。prod は骨格の構築までとし、Bastion を使う疎通確認は行っていません(第 3 章のパブリック IP 禁止ポリシーのため)。

### 決めた方針(以降の章の前提)

| 項目 | 決定内容 |
|---|---|
| CIDR 設計 | `10.0.0.0/8` を用途別に分割。ハブ=`10.0.0.0/16`(将来)、dev=`10.10.0.0/16`、prod=`10.20.0.0/16`、予備=`10.30.0.0/16` 以降 |
| サブネット構成 | 各 VNet に app 用・data 用の 2 サブネット + `AzureBastionSubnet` |
| NSG の管理単位 | サブネットに寄せる(NIC 側 NSG は原則使わない) |
| NSG の既定方針 | 拒否寄り。data サブネットは app サブネットからのみ受信許可 |
| 本番の運用アクセス | Bastion(パブリック IP 必要)は prod のポリシーと衝突。踏み台・Private 接続は続巻で設計 |

### 作成したリソース

| リソース種別 | 名前 | スコープ | 備考 |
|---|---|---|---|
| 仮想ネットワーク | `vnet-foundation-dev` | `rg-foundation-dev` | `10.10.0.0/16`。東日本。タグ `env=dev` |
| サブネット | `snet-app-dev` / `snet-data-dev` / `AzureBastionSubnet` | `vnet-foundation-dev` | `10.10.1.0/24` / `10.10.2.0/24` / `10.10.255.0/26` |
| NSG | `nsg-app-dev` / `nsg-data-dev` | `rg-foundation-dev` | 各サブネットへ関連付け。data は app からのみ許可 |
| 仮想ネットワーク | `vnet-foundation-prod` | `rg-foundation-prod` | `10.20.0.0/16`。東日本。タグ `env=prod` |
| サブネット | `snet-app-prod` / `snet-data-prod` / `AzureBastionSubnet` | `vnet-foundation-prod` | `10.20.1.0/24` / `10.20.2.0/24` / `10.20.255.0/26` |
| NSG | `nsg-app-prod` / `nsg-data-prod` | `rg-foundation-prod` | 各サブネットへ関連付け |

> 疎通確認で使った一時 VM(`vm-nettest-dev`)・Azure Bastion(`bastion-foundation-dev`)・パブリック IP(`pip-bastion-dev`)は、確認後に削除済み(常設リソースではない)。

### RBAC・ポリシー変更

| 対象 | 変更内容 | スコープ |
|---|---|---|
| — | この章では未実施(第 3 章のパブリック IP 禁止ポリシーが prod の Bastion 構築を制約する点のみ確認) | — |

### ネットワーク変更

| 対象 | 変更内容 |
|---|---|
| dev | `vnet-foundation-dev`(`10.10.0.0/16`)+ app/data/Bastion サブネット + NSG を構築 |
| prod | `vnet-foundation-prod`(`10.20.0.0/16`)+ app/data/Bastion サブネット + NSG を構築 |
| NSG 方針 | サブネット単位で管理。既定拒否寄り、data は app サブネット(`snet-app`)からのみ受信許可 |

### コスト影響

| 項目 | 概算 | 備考 |
|---|---|---|
| VNet / サブネット / NSG | 無料 | ネットワークの器自体に課金はない |
| Azure Bastion / 仮想マシン / パブリック IP | 有料 | 疎通確認でのみ使用し削除済み。置きっぱなしは課金継続。<!-- 要検証: 公式Doc確認 — 各サービスの課金体系 --> |

## 第5章 終了時点

第 5 章では、統制・ネットワークの層(第 1〜4 章)の上に「監視とコストの目」を足しました。ログの集約先を **1 つに集約**する方針を採りました。統制の効いた prod サブスクリプションに、監視用リソースグループ `rg-monitor-foundation` と Log Analytics ワークスペース `log-foundation` をただ 1 つ作成しています。dev/prod 両方のアクティビティログを診断設定でこのワークスペースへ送り、「新しく作るリソースは必ずここへ送る」という規約を確立しました(自動化は `DeployIfNotExists` ポリシーで可能なことまでを提示。本格運用は続巻)。あわせて、dev/prod それぞれに月額の予算アラートを設定し、学習用の課金事故に備えています。

### 決めた方針(以降の章の前提)

| 項目 | 決定内容 |
|---|---|
| ログの集約方針 | ワークスペースは 1 つに集約。分割は明確な理由がある時だけ |
| 集約先の置き場所 | 統制の効いた prod サブスクリプション(実運用の管理サブスクリプションを圧縮した位置づけ) |
| 診断設定の規約 | 新規リソースは必ず `log-foundation` へ診断ログを送る。徹底は `DeployIfNotExists` ポリシーで自動化可能 |
| ログ保持期間 | 目的から逆算し、短めの既定から始めて必要時に延ばす |
| 予算アラート | サブスクリプションごとに月額予算を置き、50% / 80% / 100% でメール通知 |

### 作成したリソース

| リソース種別 | 名前 | スコープ | 備考 |
|---|---|---|---|
| リソースグループ | `rg-monitor-foundation` | サブスクリプション(prod) | 監視専用。東日本。タグ `env=prod` / `owner` |
| Log Analytics ワークスペース | `log-foundation` | `rg-monitor-foundation` | 土台全体のログ集約先(ただ 1 つ)。東日本 |
| 診断設定(サブスクリプション) | `to-log-foundation` | `sub-foundation-dev` / `sub-foundation-prod` | アクティビティログを `log-foundation` へ送る |
| 予算 | `budget-foundation-dev` / `budget-foundation-prod` | 各サブスクリプション | 月額予算 + しきい値メール通知 |

### RBAC・ポリシー変更

| 対象 | 変更内容 | スコープ |
|---|---|---|
| — | この章では RBAC 変更なし(開発者は既存の prod 閲覧者権限でログを閲覧可・削除不可) | — |
| 診断設定の自動付与(`DeployIfNotExists`) | 規約の自動化として提示。割り当て時はマネージド ID とロール付与、修復タスクが必要(本格運用は続巻) | 管理グループ `mg-foundation`(想定) |

### ネットワーク変更

| 対象 | 変更内容 |
|---|---|
| — | この章では未実施(第 4 章の NSG フローログは、同じ要領で `log-foundation` へ送る対象に含める) |

### コスト影響

| 項目 | 概算 | 備考 |
|---|---|---|
| Log Analytics ワークスペース | 有料(取り込み量・保持期間で課金) | 「全部・無期限」は避け、必要なログ・期間に絞る。<!-- 要検証: 公式Doc確認 — 取り込み従量・保持課金・無料枠 --> |
| アクティビティログの取り込み | 概ね無償枠内 | アクティビティログ自体のワークスペース取り込みは低コスト。<!-- 要検証: 公式Doc確認 --> |
| 予算・Cost Management(分析) | 無料 | 予算アラート機能自体に課金はない |

## 第6章 終了時点

第 6 章では、手作業で積み上げた土台に「再現性(IaC)の型」を加えました。新しい Azure リソースは追加していません。第 4 章の dev VNet 一式(VNet・app/data/Bastion サブネット・NSG)を Bicep で表し直しました。リポジトリには設計図 `vnet-foundation-dev.bicep` を置いています。学習として VNet を一度削除し、Bicep から再構築して「コードから同じものが甦る」再現性を体験しています(再構築後の実物は第 5 章の到達状態と同一)。モジュール化・パイプライン統合(CI/CD)は本章では扱わず、続巻の終盤に送る二段構えとしました。

### 決めた方針(以降の章の前提)

| 項目 | 決定内容 |
|---|---|
| IaC の主軸 | Bicep を主軸とする(純正・状態ファイル不要・新リソースへの当日対応)。Terraform は必要時に併用 |
| 状態管理 | 状態は ARM(スコープの実物)が持つ。外部の状態ファイルは持たない |
| Bicep の作り方 | ゼロから書かず「ポータル/CLI で作る → エクスポート → decompile → 整形」を基本ワークフローとする |
| 適用前の安全確認 | デプロイ前に必ず `what-if` で差分を確認してから `create` する |
| 一括削除の単位 | CFn スタック相当の「まとめて削除」はリソースグループ削除で行う |
| コード化の範囲(本書) | 本章は dev の VNet 一式のみ。ID・ポリシー・監視の Bicep 化とモジュール化は続巻 |
| コードの必須事項 | Bicep にも第 3 章の必須タグ(`env` / `owner`)を必ず含める(prod では拒否ポリシーに弾かれるため) |

### 作成したリソース

| リソース種別 | 名前 | スコープ | 備考 |
|---|---|---|---|
| Bicep 設計図(リポジトリ) | `vnet-foundation-dev.bicep` | リポジトリ管理 | 第 4 章の dev VNet 一式をパラメータ化(`env` / 各アドレス空間 / `owner`)。Azure 上の新規リソースではない |

> Azure 上に新しく常設されるリソースはありません。ハンズオンで `vnet-foundation-dev` を削除→Bicep から再構築しましたが、再構築後の構成は第 4 章と同一(VNet `10.10.0.0/16` + app/data/Bastion サブネット + NSG)です。

### RBAC・ポリシー変更

| 対象 | 変更内容 | スコープ |
|---|---|---|
| — | この章では未実施(Bicep デプロイも ARM を通るため、既存の RBAC・ポリシーがそのまま適用される点のみ確認) | — |

### ネットワーク変更

| 対象 | 変更内容 |
|---|---|
| dev | `vnet-foundation-dev` を一度削除し、Bicep から再構築(構成は第 4 章と同一)。常設構成としての変化はなし |
| prod | 変更なし(同じ設計図を `env=prod`・`10.20.0.0/16` で流用可能なことを提示するにとどめる) |

### コスト影響

| 項目 | 概算 | 備考 |
|---|---|---|
| Bicep / ARM / what-if(IaC の道具立て) | 無料 | 課金はデプロイされる実物リソースからのみ発生する |
| VNet / サブネット / NSG の削除・再作成 | 無料 | ネットワークの器自体は無料。<!-- 要検証: 公式Doc確認 — VNet/サブネット/NSG の課金有無 --> |

## 第7章 終了時点(到達状態)

第 7 章は総仕上げの章で、**Azure 上に新しいリソースを 1 つも追加していません**。第 1〜6 章で積み上げた土台(ミニランディングゾーン)を、1 枚の全体構成図(本文の図7-5)に描き起こしました。CAF(Cloud Adoption Framework)のランディングゾーン設計領域・AWS の Control Tower / LZA・Well-Architected Framework の 5 本柱と対応づけて振り返りました。構築手順は、読み取り専用のセルフ監査(`list`/`show` 系コマンドで図7-5 と実環境の一致を確認)と、リポジトリ整理(state.md・Bicep・要検証マーカーの棚卸し)にとどめています。したがって、本章終了時点の到達状態は**第 6 章終了時点と同一**です。

### 決めた方針(以降の巻の前提)

| 項目 | 決定内容 |
|---|---|
| 完成の定義 | 「載せるアプリが決まる前に決めるべきことを決め切った」状態を土台の完成とする(アプリ非依存の部分を land させる) |
| 到達状態の単一情報源 | 全体構成図(図7-5)・章ごとのチェックリスト(図7-6)・本 state.md の 3 つを一致させ、続巻はこの to-be を出発点にする |
| 続巻へ送る宿題 | PIM の有効化・Private Endpoint・DR 設計・CI/CD / モジュール化は、載せるアプリと一緒に続巻で扱う |
| 続巻への分岐 | 同じ土台の上に、公開サービス編 / 社内 DX 編 / アプリケーションアーキテクチャ編がそれぞれ異なるアプリを載せる |

### 作成したリソース

| リソース種別 | 名前 | スコープ | 備考 |
|---|---|---|---|
| — | — | — | この章では Azure 上に新規リソースを作成していない(総仕上げ・到達状態のセルフ監査のみ) |

> 到達状態は第 6 章と同一です(本文の図7-5 が本書全体のリソース・設定の一覧)。テナントの Entra ID グループ 3 つ、管理グループ `mg-foundation`、dev/prod サブスクリプションと `rg-foundation-dev`/`-prod`/`rg-monitor-foundation`、dev/prod の VNet 一式、`log-foundation`、予算、リポジトリの `vnet-foundation-dev.bicep`——これらがそろっていることをセルフ監査で確認する構成です。

### RBAC・ポリシー変更

| 対象 | 変更内容 | スコープ |
|---|---|---|
| — | この章では未実施(既存の RBAC・ポリシーが図7-5 のとおり保たれていることをセルフ監査で確認するのみ) | — |

### ネットワーク変更

| 対象 | 変更内容 |
|---|---|
| — | この章では未実施(dev/prod の VNet・サブネット・NSG が第 4 章のとおり保たれていることを確認するのみ) |

### コスト影響

| 項目 | 概算 | 備考 |
|---|---|---|
| セルフ監査(`list`/`show` 系コマンド) | 無料 | 環境を変更しない参照操作のみ。課金は発生しない |
| 消し忘れリソースの点検 | — | 第 4 章の疎通確認で使った Azure Bastion・仮想マシン・パブリック IP が残っていないことを確認し、置きっぱなしの課金を防ぐ |
